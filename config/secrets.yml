# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

default: &default
  jwt_secret: <%= ENV["JWT_SECRET"] %>

development:
  <<: *default
  secret_key_base: 3ffb104e9faf5e60d199359fba7321caeb9618373afa79ed041339b082e137ac6f4d4202307b0a9f5e66ed457d2caf871fb0ac979687737f31ac71d88c1d2dc6

test:
  <<: *default
  secret_key_base: 3115abbe881d6e04672c8519ea0d16b4d00ebd27b64f1ea1405659d521c4c9a9fb627789458caa62543efa996634007935e3c0b92ebd6b2d3838b0c4fc1a67f6

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  <<: *default
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
